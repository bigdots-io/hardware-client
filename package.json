{
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "scripts": {
    "back:dev": "cd server && npm run emulate",
    "front:dev": "cd app && npm run dev",
    "dev": "concurrently --kill-others \"npm run back:dev\" \"npm run front:dev\""
  }
}
